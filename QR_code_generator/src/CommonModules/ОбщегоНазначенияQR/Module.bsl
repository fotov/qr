Функция РазделитьСтрокуНаЧисла(СтрокаДляРазделения) Экспорт
	
	Результат = Новый Массив;
	
	Для каждого Значение Из СтрРазделить(СтрокаДляРазделения, ",") Цикл
		
		Если Не ЗначениеЗаполнено(Значение) Тогда
		
			Продолжить;
		
		КонецЕсли;
		
		Результат.Добавить(Число(Значение));
		
	КонецЦикла; 
	
	Возврат Результат;
	
КонецФункции
